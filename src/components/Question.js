const Question = ({ questions }) => {
    return (
        <div className="container">

            <h4 className="label mb-4">{questions.title}</h4>
            
        </div>
    )
}

export default Question
